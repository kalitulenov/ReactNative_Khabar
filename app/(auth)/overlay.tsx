/**
 * КОМПОНЕНТ ЗАГРУЗОЧНОГО ЭКРАНА / OVERLAY
 *
 * Основное назначение:
 * Полноэкранный оверлей (наложение) для отображения во время:
 * 1. Загрузки приложения
 * 2. Выполнения фоновых операций
 * 3. Процесса аутентификации
 * 4. Инициализации данных
 *
 * Особенности:
 * - Полноэкранное затемненное покрытие
 * - Центрированная иконка запуска
 * - Минималистичный дизайн
 */

import { MaterialCommunityIcons } from "@expo/vector-icons";
import React from "react";
import { StyleSheet, View } from "react-native";

/**
 * Компонент Overlay - полноэкранное наложение
 * @returns JSX элемент оверлея с иконкой ракеты
 */
const overlay = () => {
  return (
    /**
     * Основной контейнер оверлея
     *
     * Стилевые свойства:
     * - flex: 1 - занимает всё доступное пространство
     * - justifyContent: "center" - вертикальное центрирование содержимого
     * - alignItems: "center" - горизонтальное центрирование содержимого
     * - backgroundColor: "#000" - черный фон с прозрачностью 70%
     *   (в данном случае без прозрачности, можно изменить на "rgba(0,0,0,0.7)")
     */
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        backgroundColor: "#000", // Можно использовать "rgba(0,0,0,0.7)" для полупрозрачности
      }}
    >
      {/**
       * Иконка ракеты как визуальный индикатор процесса
       *
       * Параметры иконки:
       * - name: "rocket-launch-outline" - контурная иконка запуска ракеты
       * - size: 100 - размер иконки в пикселях
       * - color: "white" - белый цвет для контраста на черном фоне
       *
       * Семантическое значение:
       * Ракета символизирует:
       * 1. Запуск/старт процесса
       * 2. Высокую скорость выполнения
       * 3. Инновационный подход
       * 4. Прогресс и движение вперед
       */}
      <MaterialCommunityIcons
        name="rocket-launch-outline"
        size={100}
        color="white"
      />
    </View>
  );
};

export default overlay;

/**
 * Стили компонента (в данном случае пустые, так как используется inline стиль)
 *
 * Примечание: Inline стили используются для простого компонента,
 * но для более сложных случаев рекомендуется выносить стили в StyleSheet
 * для оптимизации производительности.
 */
const styles = StyleSheet.create({});

/**
 * ВАРИАНТЫ ИСПОЛЬЗОВАНИЯ КОМПОНЕНТА:
 *
 * 1. Как экран загрузки приложения:
 *    - Отображается при инициализации
 *    - Скрывается после готовности данных
 *
 * 2. Как модальное окно:
 *    - Поверх основного контента
 *    - Блокирует взаимодействие с интерфейсом
 *
 * 3. Как индикатор процесса:
 *    - Во время сетевых запросов
 *    - При обработке данных
 */

/**
 * РЕКОМЕНДАЦИИ ПО УЛУЧШЕНИЮ:
 *
 * 1. Добавить анимацию:
 *    - Вращение иконки
 *    - Пульсирующий эффект
 *    - Плавное появление/исчезновение
 *
 * 2. Добавить текстовую информацию:
 *    - Прогресс бар
 *    - Процент выполнения
 *    - Статусное сообщение
 *
 * 3. Улучшить доступность:
 *    - Добавить accessibilityLabel
 *    - Поддержка VoiceOver
 *
 * 4. Добавить варианты темы:
 *    - Светлый/темный режим
 *    - Настраиваемые цвета
 */

/**
 * ТЕХНИЧЕСКИЕ ПРИМЕЧАНИЯ:
 *
 * 1. MaterialCommunityIcons:
 *    - Библиотека векторных иконок
 *    - Поддерживает Expo
 *    - Широкий выбор иконок
 *
 * 2. Inline стили vs StyleSheet:
 *    - StyleSheet оптимизирует производительность
 *    - Inline стили проще для прототипирования
 *
 * 3. Полноэкранный режим:
 *    - Занимает весь экран
 *    - Может перекрывать StatusBar
 *    - Рекомендуется использовать SafeAreaView для iOS
 */

/**
 * ПРИМЕР РАСШИРЕННОЙ ВЕРСИИ:
 *
 * const overlay = () => {
 *   return (
 *     <View style={styles.container}>
 *       <ActivityIndicator size="large" color="#FFFFFF" />
 *       <Text style={styles.text}>Загрузка...</Text>
 *       <MaterialCommunityIcons
 *         name="rocket-launch-outline"
 *         size={100}
 *         color="white"
 *       />
 *     </View>
 *   );
 * };
 *
 * const styles = StyleSheet.create({
 *   container: {
 *     ...StyleSheet.absoluteFillObject,
 *     backgroundColor: 'rgba(0,0,0,0.7)',
 *     justifyContent: 'center',
 *     alignItems: 'center',
 *   },
 *   text: {
 *     color: 'white',
 *     fontSize: 18,
 *     marginTop: 20,
 *   },
 * });
 */
